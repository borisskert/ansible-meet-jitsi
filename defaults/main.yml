---
volume: ''

publish:
  web_interface: 0.0.0.0
  videobridge_interface: 0.0.0.0
  web: '' # Web Port
  https: '' # Https port (not really used, but used)
  url: '' # Public url

tz: Europe/Berlin

users: []

enable_auth: yes
allow_guests: no

force: no
